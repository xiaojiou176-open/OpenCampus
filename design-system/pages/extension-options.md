# Extension Options Override

## Purpose

这页是诚实配置台，不是模型实验室。

## Override Rules

1. 顺序固定为：
   - `Boundary disclosure`
   - `AI/BFF status summary`
   - `Site configuration`
   - `Advanced runtime settings`
   - `Export defaults`
2. provider readiness 做成 compact status list
3. `OpenAI / Gemini / Switchyard model` 默认归入 advanced
4. 保存按钮固定在表单底部，并显示 unsaved state

## Copy Rules

- `Only override when active-tab inference fails.`
- 所有 boundary 文案必须在第一屏可见
